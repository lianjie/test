<%= form_for(@point) do |point_form| %>
  <% if @point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@point.errors.count, "error") %> prohibited this point from being saved:</h2>

      <ul>
      <% @point.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= point_form.label :lon %><br />
    <%= point_form.text_field :lon %>
  </div>
  <div class="field">
    <%= point_form.label :lat %><br />
    <%= point_form.text_field :lat %>
  </div>
  
  <div class="actions">
    <%= point_form.submit %>
  </div>
<% end %>

