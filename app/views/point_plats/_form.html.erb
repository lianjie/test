<%= form_for(@point_plat) do |f| %>
  <% if @point_plat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@point_plat.errors.count, "error") %> prohibited this point_plat from being saved:</h2>

      <ul>
      <% @point_plat.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :lat %><br />
    <%= f.text_field :lat %>
  </div>
  <div class="field">
    <%= f.label :lon %><br />
    <%= f.text_field :lon %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
